- 前端采用自定义 Hooks（如 `useWorkspaces`, `useAgentContext`）封装复杂的状态管理与业务逻辑。
- Rust 后端通过 `#[tauri::command]` 宏暴露原子化功能接口，并在 `lib.rs` 中统一注册。
- UI 组件遵循模块化拆分，按功能域（如 `agent/`, `settings/`, `wiki/`）组织在 `src/components/` 下。