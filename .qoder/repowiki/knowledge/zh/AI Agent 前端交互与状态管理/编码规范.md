- 采用“容器-展示”模式，将复杂的业务逻辑（如 Agent 消息解析、状态更新）封装在 `hooks/` 下的自定义 Hook 中，组件仅负责 UI 渲染。
- 使用 `useRef` 存储回调函数或定时器 ID，以避免在 `useEffect` 依赖变化时重复注册监听器或丢失引用。
- 在涉及异步数据加载或 Sidecar 连接的场景下，统一提供 `isLoading` 状态以展示骨架屏或 Loading 动画，确保用户体验平滑。