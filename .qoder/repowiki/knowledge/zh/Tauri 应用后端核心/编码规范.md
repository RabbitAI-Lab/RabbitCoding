- 所有耗时或阻塞操作（如 Wiki 生成、Sidecar 通信）均封装为 `#[tauri::command]`，并通过 `Result<T, String>` 统一返回错误信息。
- Sidecar 及插件安装命令严格注入 `CLAUDE_CONFIG_DIR` 环境变量，将 Claude 配置重定向至应用专用目录，确保与用户全局环境隔离。
- Wiki 生成模块采用“元数据断点续传”模式，通过 `_meta.json` 记录各阶段进度，支持任务中断后的自动恢复与失败项重试。