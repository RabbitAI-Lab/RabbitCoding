- **混合架构编排**：作为 Tauri 应用的根目录，通过 `vite.config.ts` 配置固定端口（1420）与 HMR 协议，协调 React 前端开发与 Rust 后端运行时环境。
- **多语言工作流集成**：利用 `pnpm` 脚本（如 `setup:resources`）串联 Node.js 资源准备与 Tauri CLI 构建流程，实现前端 TS/React、后端 Rust 及 Sidecar 子进程的协同启动与打包。
- **跨进程通信契约**：定义并维护前端（`@tauri-apps/api`）、后端（Tauri Commands）与 Sidecar（stdin/stdout JSON-lines）之间的数据交换标准，确保 AI 流式响应与系统级指令的稳定传输。