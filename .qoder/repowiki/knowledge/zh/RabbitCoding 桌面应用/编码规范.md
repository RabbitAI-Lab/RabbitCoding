- 前端通过自定义 Hooks（如 `useAuth.tsx`, `useWorkspaces.ts`）封装 Tauri Invoke 调用与本地状态管理，实现逻辑复用。
- Rust 后端采用模块化设计，每个功能域（如 `wiki.rs`, `voice.rs`）独立管理其状态与命令，并通过 `lib.rs` 统一注册到 Tauri Builder。
- 使用 TypeScript 接口定义前后端通信的数据结构，确保 `invoke` 调用的类型安全。