- **环境依赖**：需配置 `APPLE_API_KEY_CONTENT`, `TAURI_SIGNING_PRIVATE_KEY` 等 Secrets 以完成签名流程。
- **资源初始化**：执行 `node sidecar/scripts/setup-resources.mjs --no-bundle` 准备 Sidecar 环境。
- **并发控制**：同一分支的新推送会自动取消正在进行的旧构建 (`cancel-in-progress: true`)。