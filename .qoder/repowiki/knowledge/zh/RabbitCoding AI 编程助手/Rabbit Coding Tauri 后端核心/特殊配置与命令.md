- **构建前准备**：需在根目录执行 `pnpm build` 编译前端资源。
- **Sidecar 依赖**：生产环境需预编译 `sidecar-bundle.js` 并置于 `resources/sidecar/`；开发环境通过 `npx tsx` 直接运行 TypeScript 源码。
- **Node 运行时注入**：`build.rs` 或 `setup` 逻辑会将 `resources/node-runtime` 下的二进制文件路径注入 `PATH`，以支持跨平台的 Node 环境隔离。