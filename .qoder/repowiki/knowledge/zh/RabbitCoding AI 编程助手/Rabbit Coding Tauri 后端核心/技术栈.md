- **Tauri v2**：桌面应用框架，启用 `macos-private-api` 和 `devtools`。
- **sherpa-onnx**：用于离线语音识别（Silero VAD + SenseVoice），通过条件编译排除 Windows ARM64。
- **rusqlite (bundled)**：嵌入式 SQLite 数据库，用于存储工作区状态和会话历史。
- **reqwest & tokio**：处理 HTTP 请求（如 OAuth 交换、模型下载）和异步运行时。