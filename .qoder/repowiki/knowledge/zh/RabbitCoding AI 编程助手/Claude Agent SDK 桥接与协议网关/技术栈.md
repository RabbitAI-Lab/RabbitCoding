- **Runtime**: Node.js ESM (`"type": "module"`)。
- **SDK**: `@anthropic-ai/claude-agent-sdk` (v0.3.177) 用于驱动 AI Agent 生命周期。
- **Build**: `esbuild` 进行快速 Bundle，`typescript` 进行类型检查。
- **Schema**: `zod` 用于运行时数据校验（如 Tool Input）。