- **Runtime**: Node.js (ESM, via `type: module` in `package.json`).
- **Bundler**: `esbuild` for production bundling (`dist/sidecar-bundle.js`).
- **SDK Integration**: `@anthropic-ai/claude-agent-sdk` with platform-specific native binary resolution.