- **Tauri v2**：桌面应用框架，启用 `unstable` 与 `devtools` 特性。
- **rusqlite (bundled)**：嵌入式 SQLite 数据库，用于工作区与对话历史的本地持久化。
- **reqwest & tokio**：异步 HTTP 客户端与运行时，用于 OAuth 交换、网络诊断及 API 交互。
- **std::process**：直接操作系统子进程，用于管理 Sidecar、Node.js 工具链及系统诊断命令（ping/dig/curl）。