- **构建前置条件**：需在根目录执行 `pnpm build` 编译前端资源，随后 Tauri 将其打包进应用。
- **生产环境注入**：`lib.rs` 在非调试模式下自动将 `resources/node-runtime` 注入进程环境变量，确保打包后的应用能独立运行 Node.js 脚本。
- **Sidecar 架构**：通过 `resources/sidecar/sidecar-bundle.js` 运行 AI 核心逻辑，支持开发模式（tsx）与生产模式（内置 node）的动态切换。