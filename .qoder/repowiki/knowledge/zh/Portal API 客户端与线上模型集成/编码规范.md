- 采用错误码标记机制（如 `NOT_AUTHENTICATED`、`AIKEY_NOT_RETURNED`），在抛出 Error 对象时附加 `code` 属性以区分业务异常场景。
- 遵循严格的密钥层级隔离，Casdoor accessToken 仅用于鉴权 `/api/me/*` 接口，而换取的 AI 转发 Key 则作为虚拟配置的 `apiKey` 用于模型请求。
- 对线上模型 ID 实施命名空间隔离，统一添加 `__online__:` 前缀，并通过辅助函数 `isOnlineModelId` 和 `extractModelIdFromOnline` 进行识别与解析。